# Faz 5 (cutover hazırlık) çok-model FINAL denetimi (2026-07-21) — opus + Codex + Gemini + Explore

Kapsam: tüm dal `f62baec..17e56e0` (Faz 1-5, 62 commit) + Faz 5 deltası derin (`d48e81b..17e56e0`, reconcile çekirdeği + script kablolaması + 2 indeks + /all studentId filtresi). Dört bağımsız denetim + controller çapraz-doğrulaması + fix dalgası.

## SONUÇ: Fix dalgasından SONRA cutover'a geçilebilir (runbook Mustafa-gate'i ile)

Yargı dağılımı: **opus** (whole-branch) ✅ geçilebilir, Critical/Important YOK; **Gemini** ✅ 5 kural TAM; **Explore** ✅ cleanup sonrası kırılacak canlı yol YOK; **Codex** ⛔ "geçilmemeli" — ama bulguları KOD değil RUNBOOK/güvenlik-rayları hakkında. Codex'in gerçek+ucuz bulguları FIX-A/B/C/D ile kapatıldı; ulaşılamaz senaryoları (aşağıda) çürütüldü. Dördü de fix sonrası "sakin pencere + dry-run gate + Mustafa onayı" ile güvenli diyor.

## Dördünün bağımsız doğruladıkları (güvenli)
- **reconcile karar mantığı saf çekirdekte** (reconcileSablonDeletes/reconcileReservationOps, 14+1 test); script yalnız veri çeker + uygular + raporlar (opus + Codex + Gemini).
- **JSON-authoritative + `bookedById!=='migration'` koruması** her yolda (soft-delete/cancel/tableOnly) — post-deploy/smoke yazımları asla ezilmez (dördü).
- **dry-run sıfır DB yazma** (tüm create/update/upsert/soft-delete `if(APPLY)` arkasında — satır satır izlendi: Codex + Gemini).
- **geçmiş haftaya yazma yok** (`weekKey >= currentWeek`), **bookedAt korunur** update'te (Codex + Gemini).
- **Sec1 atomik $transaction** (şablon soft-delete + migration iptalleri tek batch) — yarı-silinmiş şablon imkânsız (opus + Gemini + T2 re-review).
- **cleanup sonrası canlı JSON `etutSablonlari` okuyan yol YOK** — tüm `etutSablonlari` referansları: (a) program/route.ts write-preserve pass-through `if(!==undefined)` guard'lı (cleanup→undefined→dal atlanır), (b) göç/cleanup/rollback scriptleri, (c) yorum. `getAllProgramTemplates` çağrılmıyor (ölü tanım). (Explore + opus).
- **Faz-arası bütünlük:** resolveEffective/allowedBookingWeeks/kilit sırası (slotweek→slot→student)/kimlik uzayları (legacyId vs CUID) Faz 5'te bozulmamış; /all değişikliği additive (web+mobil sözleşme drift'i yok). Fast-forward merge temiz (main==merge-base). (opus).
- **Mobil + e2e + vitest** tablo-API'siyle; JSON varsayan bayat fikstür/test yok (Explore).

## Bulgular ve kapanışları

| Bulgu | Kaynak | Kapanış |
|---|---|---|
| **GERÇEK: reconcile exit gate `invalidSablon`+`studentIdMissing` kovalarını atlıyor** (satır 197 yalnız conflict/unresolved/writeFailed) — atlanmış bozuk/öğrencisiz şablonla exit 0 → cleanup tek JSON kopyasını siler → KALICI KAYIP | Codex K2/O9 | ✅ FIX-B: reconcile exit gate'e invalidSablon+studentIdMissing eklendi (non-reconcile gate ile parite). |
| **GERÇEK: cleanup exit-code'a bakmadan tek JSON kopyasını silebilir** | Codex K2/K3 | ✅ FIX-A: cleanup-etut-json.mjs son reconcile raporunu okur; KAYIP-riskli kova (unresolved/invalidSablon/studentIdMissing/writeFailed) BOŞ DEĞİLSE veya rapor yoksa exit 1 (temizlik reddedilir; `--force` + gerekçe ile bilinçli geçiş). KAYIPSIZ kovalar (conflict/tableOnly/recurringPresent/skipRecent) yalnız uyarı — tabloda veri var, JSON silmek kayıp değil. |
| **GERÇEK ama dar: recurring table-first, JSON WEEK create ile GÖLGELENİR** (fiziksel dokunmaz, efektif ezer) — recurring yalnız post-deploy müdür UI'dan | Codex Y5 | ✅ FIX-C: reconcileReservationOps — create hedefinde aynı şablonda table-first RECURRING varsa 'create' YERİNE 'conflict-recurring' (gölgeleme yok) + test. |
| **rollback gerçekçiliği: JSON restore SIRASI + eski deploy** | Codex K3/#6 | ✅ FIX-D runbook: JSON restore ÖNCE → eski deployment promote SONRA (eski-kod-boş-JSON penceresi yok). Cutover-sonrası SQL yazımları JSON'a dönmez = Mustafa'nın kabul ettiği kısa-pencere kaybı (belgeli). |
| **operasyonel: reconcile org-genelinde → testkurs churn** (Faz 2b smoke tablo-first kalıntıları soft-delete görür) | opus O1 | ✅ FIX-D runbook: gerçek apply `--org akyazicozum` (--org öğretmen listesini scope'lar → testkurs dokunulmaz, controller teyit etti); testkurs ayrı/bilinçli-kabul. |
| **KRİTİK(Codex) şablon soft-delete migrated-olmayan rezervasyonu gizler** | Codex K1/Y4 | ÇÜRÜTÜLDÜ (opus mutabık, Minor): tek-geçiş cutover'da JSON temizlenene DEK migrated şablon JSON'da → soft-delete ADAYı olmaz. Senaryo yalnız JSON boşaldıktan/reconcile günler sonra tekrar koşulursa ulaşılır → T6 "cleanup sonrası ASLA reconcile" notu + FIX-D. |
| reconcile atomik snapshot değil (reconcile#1 sırasında JSON yazımı) | Codex Y6 | KABUL: iki-geçiş + FIX-A gate telafi eder (silinen her şey tabloda güvende); reconcile#2 deploy READY'den DAKİKALAR içinde (60dk guard) koşulur. Runbook notu. |
| cleanup/rollback programTemplate read-modify-write lost-update (grid ile) | Codex Y7 | KABUL: sakin pencere şartı + runbook notu (küçük ölçek, dakikalar). |
| upsert koşulsuz → "2. koşu sıfır SQL" değil (net veri değişimi sıfır) | Codex O8 | KABUL: kozmetik (updatedAt); idempotency net-veri düzeyinde garantili (opus + T2 re-review). |
| 60dk createdAt guard yalnız runbook penceresi | Codex Y4 / opus (c) | KABUL/belgeli: reconcile#2 dakikalar içinde koşulmalı; script yorumunda + T6. |
| ölü kod: StudentList default export, SinifOgrenci kullanılmayan prop, lib/slots.ts EtutSablonu+etutAktifThisWeek, program/route.ts:193 ulaşılamaz dal | opus (a)/(f)/(b) | T6 cutover-sonrası temizliğe (planla tutarlı; merge-blocker değil). |
| /all boş-string studentId; hafta-içi gün sıralaması; active[0] determinizmi | opus (d)/(e)/(g) | KABUL: kabul-edilebilir/kozmetik/ulaşılamaz (tek-hafta göçü ≤1 gelecek ACTIVE satır). |
| grid `type:'etut'` savunmacı okuma dalları initWeekForTeacher'a bağlı (dual-source riski) | Explore not | KAPSAM DIŞI: etutSablonlari cleanup'ının değil; reconcile hayalet-tarama (rapor-only) bunu yüzeye çıkarır — ayrı eski-veri temizliği (T6). |

## Fix dalgası (FIX-A/B/C kod, FIX-D runbook)
- FIX-A: `scripts/cleanup-etut-json.mjs` reconcile-rapor gate.
- FIX-B: `scripts/migrate-etut-to-tables.mjs` reconcile exit gate genişletme.
- FIX-C: `scripts/etut-migration-lib.mjs` recurring-shadow guard + `scripts/etut-migration-lib.test.mjs`.
- FIX-D: `docs/superpowers/plans/2026-07-21-etut-hafta-bazli-faz5.md` runbook hardening (controller).

## İSTEĞE BAĞLI (bloke etmez, Mustafa'ya sunuldu)
Etüt-yazma bakım-freeze bayrağı (Redis key + write-path gate) — rollback'i post-cutover için de kayıpsız yapar; spec "bakım penceresi" bunu ima etti. Tek-geçiş + FIX-A/B/C ile ZORUNLU DEĞİL: rezidü yazımlar korumalı (non-migration ezilmez), migrated şablon silinmez, cleanup gate kayıp-riskini keser. Ölçek küçük (akyazicozum ~63 şablon/2 rez), pencere dakikalar.

## Süreç notları
- opus en isabetli kalibrasyon (Codex'in KRİTİK'ini Minor'a indirdi, gerekçesi tek-geçiş cutover); Codex en derin güvenlik-rayı taraması (exit-gate + cleanup gate); Gemini kod-kuralı doğrulaması; Explore cleanup-safe okuma envanteri. Dörtlü kombinasyon: kod-doğru + runbook-eksik ayrımını netleştirdi.
- Gemini bu turda yanlış-pozitif YOK (Faz 3'teki sorun tekrarlamadı).
- Vercel CLI 56.2.1 → deploy öncesi `npm i -g vercel@latest` (Codex + oturum hook'u) — FIX-D pre-check.
