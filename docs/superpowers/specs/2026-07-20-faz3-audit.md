# Faz 3 çok-model denetimi (2026-07-20) — Codex + Gemini + Explore

Kapsam: `git diff 6a3fd55..4d00c9c` (Faz 3: okuma yolları JSON→tablo). Üç bağımsız denetim + controller çapraz-doğrulaması.

## ORTAK YARGI (3/3): Kritik/Yüksek gerçek bulgu YOK — FAZ 4'E GEÇİLEBİLİR

(Codex koşulu: ileri-hafta iptal ürün kararı netleşsin — aşağıda, Mustafa'ya soruldu.)

Üçü de bağımsız doğruladı:
- **(a) Dört okuma yolu efektif kural TUTARLI:** `/all` (listEtutlerForWeek), listSablonlarWithRez, listBookableEtuts, mobil today/week — hepsi resolveEffective (WEEK-ezer-RECURRING + tombstone + effectiveFromWeek) + `deletedAt:null`. Tek bilinçli asimetri: listSablonlarWithRez pasifleri süzmez (ProgramEditor gösterir).
- **(b) Pencere sunucu-otoriter:** istemcilerde TSİ/saat hesabı YOK (yalnız bookableWeeks string kıyası); UI atlatılsa da decideBooking kural 4 → 403. "Pazar 11:00" yalnız statik açıklama metni.
- **(c) PATCH geriye uyumlu:** scope'suz eski istemci = eski RECURRING davranışı birebir.
- **(d) Sözleşme kırılması YOK:** tüm eklemeler additive; mobil eski binary `Array.isArray` kalkanıyla güvenli; api-types byte-senkron.
- **(f) Performans:** N+1 yok; eski öğretmen-başına JSON sorgusu yerine sabit sorgu sayısı (net iyileşme).
- **Kimlik uzayları (Explore):** join'ler sablon CUID üzerinden, dışa yalnız legacyId; teacherId=legacyId eşleşmeleri şemayla birebir. e2e `ui-etut.spec.js` fikstürü tablo API'siyle tutarlı.
- **Kör nokta taraması (Explore):** bilinenler dışında bayat etüt okuyucusu KALMADI; `getAllProgramTemplates` artık hiç çağrılmıyor (yalnız tanım).

## Bulgular ve kapanışları

| Bulgu | Kaynak | Karar/Kapanış |
|---|---|---|
| **ORTA (ürün kararı):** öğrenci, müdürün İLERİ haftaya koyduğu rezervasyonu penceresi açılmadan iptal edebiliyor (cancelEtutV2'de pencere kapısı yok) | Codex Orta; Gemini "risk değil, bilinçli esneklik"; Explore not | **Mustafa'ya soruldu** (AskUserQuestion, Faz 3 kapanışı). Eski JSON dönemi davranışı da iptal-serbestti; cancelLockHours koruması ayrıca var. Karar Faz 4 girişine işlenecek. |
| **DÜŞÜK:** GET `/all` + GET `/api/etut-sablon` week paramını doğrulamıyor (çöp weekKey → recurring yanlış görünüm) | Codex (T2 review Minor'ı ile aynı) | ✅ KAPANDI — `864337c` FIX-A: param verilmişse isValidWeekKey, geçersizde 400 'Geçersiz hafta formatı'; paramsız fallback birebir eski. Re-review Approved. |
| **DÜŞÜK:** listSablonlarWithRez tenant-genelinde TÜM hafta rezervasyonlarını çekiyor | Codex | ✅ KAPANDI — `864337c` FIX-B: getWeekReservations'a opsiyonel `sablonIds` filtresi; listSablonlarWithRez öğretmenin şablon CUID'lerine daraltıldı. 7 mevcut çağıran etkilenmedi (re-review teyit; `in: []` boş-öğretmen durumu doğru). |
| **ORTA (bilinen/Faz 4):** attendance/student hâlâ bayat JSON okuyor — Faz 2b sonrası oluşturulan etütlerde branch/slotLabel boş döner (saveSablon JSON'a yazmıyor) | Explore | Faz 4 kapsamında zaten planlı ("attendance snapshot") — **Faz 4'ün İLK işi** olarak işaretlendi. |
| **DÜŞÜK (semantik):** hafta pasif işaretlenince recurring rezervasyon o hafta öğrenci-yüzü okumalarında gizli (yazma yolu FIX-D recurring'i pasifHaftalar'dan muaf tutar) | Explore | KABUL — tutarlı ürün semantiği: "o hafta öğretmen müsait değil = etüt yok"; üç öğrenci-yüzü okuma simetrik, müdür (editör) görmeye devam ediyor. Belgelendi. |
| **DÜŞÜK (kozmetik):** banner metni istemci-yerel getWeekKey'e dayanıyor (TR-dışı TZ'de metin kayabilir; KAPI sunucu-otoriter, etkilenmez) | Explore + Codex önerisi (`nextBookingOpensAt`) | KABUL — TR kullanıcı tabanında etkisiz. İleride i18n/TZ işi olursa sunucu `nextBookingOpensAt` dönebilir. |
| **YANLIŞ POZİTİF:** ProgramEditor:415 `sb.scope` kullanıyor (↻ hiç görünmez) + kaldırmada scope gönderilmiyor | Gemini Yüksek-1 + Orta-1 | Controller kodda çürüttü: satır 415 `sb.rezScope` kullanıyor; kaldırma butonları scope'u ('WEEK'/'RECURRING') açıkça geçiyor (ProgramEditor:708-712). Diff-öncesi/plan metninden okuma hatası. |

## Süreç notları
- Gemini headless ilk iki denemede izin sorununa takıldı (`command` → `read_file`); `--sandbox --dangerously-skip-permissions` kombinasyonuyla koştu. Gelecek denetimlerde doğrudan bu bayraklarla başla.
- Codex read-only sandbox'ta build/test koşamadı (statik denetim) — gate'ler zaten controller tarafında yeşildi.

## Faz 4 girişine taşınanlar
1. attendance/student → EtutReservation snapshot (Explore Orta — Faz 4'ün ilk işi).
2. İleri-hafta iptal ürün kararı (Mustafa yanıtına göre: ya `cancelEtutV2`'ye öğrenci-pencere kapısı + test, ya mevcut davranışa regresyon testi).
3. Faz 2 denetiminden ertelenmiş Y2 (recurring gelecek-hafta çakışması) + Y3 (ProgramEditor/initWeek direct SlotBooking) — Faz 4 planına dahil (Mustafa kararı, Faz 3 girişi).
4. Genel sessiz-catch süpürmesi (TeacherPanel yoklama-yükleyicisi kapandı; kalan repo geneli Faz 4).
