# Faz 4 çok-model denetimi (2026-07-21) — Codex + Gemini + Explore

Kapsam: `git diff fd8dd49..49ca565` (Faz 4: görünürlük+geçmiş). Üç bağımsız denetim + controller çapraz-doğrulaması + 2 düzeltme dalgası + re-review.

## SONUÇ: Tüm kapılar kapandı — FAZ 5 CUTOVER'A GEÇİLEBİLİR

İlk yargılar: Gemini "şartlı evet", Explore "koşullu evet", Codex "geçilmemeli — 5 asgari kapı". Kapıların tamamı FIX-1 (f608d79) + FIX-2 (14c94af) ile kapatıldı, re-review Approved (Critical/Important sıfır).

## Üçünün bağımsız doğruladıkları (güvenli)

- freezeRecurringWeek alan tamlığı (şemayla alan-alan), tombstone ASLA ezilmez (resolveEffective önceliği), idempotency (2. koşu 0), weekKey='*' sızıntısı yok.
- slotweek kilit anahtarı 3 yolda bayt-parite (Teacher.id CUID); kilit uzayları (slotweek/slot/etut salt-1, student salt-0) ve edinim sıraları deadlock'u yapısal olarak imkânsız kılıyor (7 çağrı yeri tek tek izlendi).
- attendance deletedAt-istisnası YALNIZ tarihsel etiket lookup'ında; tüm canlı listeler deletedAt:null.
- StudentEtutTab readOnly/yetki doğru; prop temizliği tam; StudentPanel/ParentPanel etkilenmedi; mobil tutarlı.
- Arşiv/geçmiş entry-şekil paritesi; ders(Redis-slotId)/etüt(etut:-prefix) ayrık merge.

## Bulgular ve kapanışları

| Bulgu | Kaynak | Kapanış |
|---|---|---|
| **KRİTİK: rollover idempotent değil** — çifte cron hafta atlatır (pre-existing; freeze sonuçları ağırlaştırdı) | Codex | ✅ FIX-1: `shouldRollWeek(stored, actual)` saf guard (stored ileri→atla; geride/eşit→devret, kaçırılmış-hafta telafisi korunur; 5 test, yıl sınırı dahil). ms-içi eşzamanlı tetik teorik kaldı (cron tek kaynaklı — kabul, yorumda; gerekirse Faz 5+ marker tablosu). |
| **YÜKSEK: freeze hatası → kalıcı tarihçe boşluğu** (hafta yine ilerliyor, retry yok) | Codex | ✅ FIX-1: self-heal — her devir son 3 haftayı idempotent dondurur (sağlıklıysa count=0). Sınır (re-review Minor): boşluk penceresinde sahip değiştiyse geçmişe güncel sahip donar — bilinçli boşluk-doldurma > mükemmel tarihsellik, belgelendi. |
| **YÜKSEK (3/3 mutabık): cari/donmamış RECURRING geçmiş listelerinde yok** — StudentEtutTab (canlı) ile HistoryModal/arşiv çelişiyordu | Codex + Gemini + Explore | ✅ FIX-2: `listEtutHistory` cari haftayı resolveEffective ile çözer (WEEK+RECURRING, RECURRING'e weekKey=cur yazılır), DB cari-WEEK satırlarının yerine koyar — çift satır yok, CANCELLED sızmaz; teacher yolu legacyId-uyumlu. |
| **YÜKSEK: slots DELETE bayat öğrenci-kilidi** (pre-tx occupant; Faz 2 FIX-1'in bilinen Minor'ının yükseltilmişi) | Codex | ✅ FIX-2: kilitler (slotweek→slot) sonrası TAZE satır okunur, student kilidi taze occupant'la alınır, sahiplik taze satıra karşı; eski `preLockStudentId` silindi; hata metinleri birebir. |
| **ORTA (3/3 mutabık): cron Promise.all(initWeek) havuz baskısı** (her initWeek artık interactive tx) | Codex + Gemini + Explore + T5 review | ✅ FIX-1: sıralı `for...of` (admin/week advanceWeek ile aynı desen; per-teacher kilitler zaten ayrık). |
| **HistoryModal currentEntries boş/undefined → cari ders satırları kaybolur** | Gemini (Yüksek dedi; gerçekte dar) | ✅ FIX-2: `currentSlots` fallback'i — currentEntries boşsa arşivin non-etut cari satırları kullanılır. Re-review erişilebilirliğini kanıtladı (TeachersTab `currentEntries=[]` geçebiliyor). |
| **reset-all EtutSablon/EtutReservation'a cascade yapmıyor** (şema yorumu + spec §8 öngörüyordu) | Explore | ✅ FIX-2: `etutSablon.deleteMany({})` ($extends tenant-scoped — sqldb SCOPE_WHERE'de doğrulandı; canlı DB'de FK `delete_rule=CASCADE` controller tarafından sorguyla TEYİT edildi). |
| Freeze/recurring-reassignment sınır belirsizliği (Pazar 11:00 ms-penceresi) | Codex Orta / Explore F5 "iyi-huylu" | KABUL — skipDuplicates + WEEK-önceliği; iki sonuç da tutarlı ("hafta kapanırken kim tutuyorsa"). |
| attendance sablon lookup'ı teacher-scope'suz legacyId | Explore F4 | GÜVENLİ — @@unique([orgSlug,branch,legacyId]) + $extends scoping; not düşüldü. |

## FAZ 5 LİSTESİNE taşınanlar

1. **Cutover çift-kaynak temizliği:** göç JSON→EtutReservation taşıdı ama eski SlotBooking slot-etüt satırlarını temizlemedi — reconcile'da raporla/temizle (arşiv çift kaydı + eski `data.fixed:false` booked satır taraması — T5 notuyla birleşik).
2. Arşiv SlotBooking kolu kararı: all-weeks indeks ekle `(orgSlug,branch,teacherId/studentId,weekKey)` VEYA cutover sonrası SlotBooking arşiv kolunu kaldır (Codex O8).
3. `/api/etut-sablon/all`'a opsiyonel `studentId` sunucu filtresi (least-privilege/performans — Codex D9 + Gemini D4).
4. Rollover ms-içi çifte-tetik marker tablosu (yalnız cron kaynağı çoğalırsa).
5. reset-all'ı canlıda ilk kullanmadan önce not: etüt şablon+rezervasyonları da siler (yeni davranış — bilinçli, spec §8).

## Süreç notları
- Gemini 2 gerçek bulgu + 1 yanlış-alarm-değil (fallback gerçekten erişilebilirmiş); Faz 3'teki yanlış-pozitif sorunu bu turda yok.
- Codex en derin taramayı yaptı (rollover idempotency'yi yalnız o gördü); Explore "reset-all cascade" ve kabul-edilebilirlik kalibrasyonunda en isabetliydi. Üçlü kombinasyon değerini yine kanıtladı.
